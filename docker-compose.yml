version: '1.0'

services:
  app:
    image: my-website
    container_name: eng.d-dimitrov.eu
    networks:
    - default
    restart: always
    volumes:
      - .:/app
    command: npm start
networks:
  default:
    external:
      name: docker_network